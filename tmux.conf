set -g mouse on
set -g default-terminal "screen-256color"
set -g default-command "${SHELL}"
set-option -gw xterm-keys on

#set -g status off
#set-option status 2

# set window title list colors
#set-window-option -g window-status-style fg=brightblue,bg=#1f2428,dim

# active window title colors
#set-window-option -g window-status-current-style fg=green,bg=colour236,dim

# set color for status bar
set-option -g status-style bg=colour235,fg=yellow,dim

# set window title list colors
set-window-option -g window-status-style fg=brightblue,bg=colour236,dim

# active window title colors
set-window-option -g window-status-current-style fg=brightred,bg=colour236,bright

set -g status-style bg='#1f2428',fg='#191d20'
#set -g status-style bg='#1f2428',fg='#fabd2f'
set -g status-interval 15
bind-key r source-file ~/.tmux.conf \; display "Reloaded config"
#bind v split-window -h
#bind h split-window
#bind O select-layout even-vertical
#bind P select-layout even-horizontal
#bind S setw synchronize-panes

set-option -g status-left-length 50
#set-option -g status-position top

#setw -g pane-border-status top

setw -g pane-border-format '─'

# splits open in same working directory
bind '"' split-window -c "#{pane_current_path}"
bind '%' split-window -h -c "#{pane_current_path}"

# creates a new window right after the current one
bind c new-window -ac "#{pane_current_path}"


set -g pane-active-border-style "bg=#1f2428 fg=#1f2428"
#set -g pane-active-border-style "bg=#1f2428 fg=#d8a657"
set -g pane-border-style "fg=#1f2428"

set -g window-style 'bg=#191d20'
set -g window-active-style 'bg=#191d20'

#set -g status-left "Tidal"
#
set-window-option -g window-status-format " #I: #W "
#set -ag status-left "#[fg=#fabd2f,bg=default] #{pane_current_path} "
#set -ag status-left "#[fg=#d8a657,bold,dim,bg=default] #(du -sh #{pane_current_path} | cut -f1) "
#set -ag status-left "#[fg=#d8a657,bold,dim,bg=default] #(git status  | cut -f1)"
#set -g status-left "#[fg=#1f2428, bold, bg=#A3BE8C]#(echo ' TIDAL ')"
set -g status-left "#{tmux_mode_indicator}"
#set -ag status-left "#[ fg=red,bold, bg=#1f2428 ] #S "
#set -ag status-left "#[bg=#1f2428, fg=#EBCB8B] #{pane_current_path} #{prefix_highlight} "
set -g @prefix_highlight_fg '#1f2428' # default is 'colour231'
set -g @prefix_highlight_bg '#ea6962'  # default is 'colour04'


#set -g status-right '#[ bg=#ea6962,bold,fg=#1f2428 ]#{pomodoro_status}'
set -g status-right "#[fg=#EBCB8B,bg=default]#{pane_current_path} "
set -ag status-right '#[ fg=#A3BE8C,bold, bg=#1f2428 ] %Y-%m-%d %H:%M '
set -ag status-right "#[ fg=red,bold, bg=#1f2428 ] #S "
#set-window-option -g window-status-format " #I: #W "

#set-window-option -g window-status-current-format " #I: #W "
#set-window-option -g window-status-current-fg green
#set-window-option -g window-status-current-bg black

# Options
#set -g @pomodoro_mins 25
#set -g @pomodoro_break_mins 5
#set -g @pomodoro_on " #[fg=$text_red]🍅 "
#set -g @pomodoro_complete " #[fg=$text_green]🍅 "

set -g @mode_indicator_empty_mode_style 'fg=green, bold, bg=#1f2428'
set -g @mode_indicator_prefix_mode_style 'fg=red,bold,bg=#1f2428'
set -g @mode_indicator_copy_mode_style 'fg=orange,bold,bg=#1f2428'

# Keybindings
#set -g @pomodoro_start 'a'
#set -g @pomodoro_cancel 'A'

bind-key -r -T copy-mode-vi Y send-keys -X begin-selection \; send-keys -X select-line \; send-keys -X copy-selection

#bind -n C-S-Left select-pane -L

#bind -n C-Left select-pane -L
#bind -n C-Right select-pane -R
#bind -n C-d killp \; select-layout even-horizontal
#unbind C-b
#set-option -g prefix Super_L

#set -g pane-border-status top
#set -g pane-border-style "bg=#1f2428, fg=#1f2428"
#set -g pane-active-border-style ""
#set -g pane-border-format "#[#{?#{pane_active},fill=yellow,bg=blue fill=blue}]"

# List of plugins

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'b0o/tmux-autoreload'
#set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
#set -g @plugin 'roosta/tmux-pop'
set -g @plugin 'sainnhe/tmux-fzf'
#set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'olimorris/tmux-pomodoro-plus'
#set -g @plugin 'tmux-plugins/tmux-sidebar'
#set -g @plugin 'tmux-plugins/tmux-yank'
#set -g @plugin 'ofirgall/tmux-window-name'
set -g @plugin 'MunifTanjim/tmux-mode-indicator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
#set -g @resurrect-strategy-nvim 'session'
#set -g @resurrect-capture-pane-contents 'on'
#set -g @resurect-processes 'nvim zsh'
#set -g @resurrect-strategy-nvim 'session'
#set -g @resurrect-processes '"vim->vim +SLoad"'
#set -g @plugin 'Determinant/tmux-colortag'
#set -g @plugin 'tmux-plugins/tmux-continuum'
#set -g @continuum-restore 'on'
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'

#set -g @sidebar-tree-command 'tree -C'
#set -g @sidebar-tree 'p'
#set -g @sidebar-tree-width '30'
#set -g @sidebar-tree-command 'tree -CL 2'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
